import "core-js/stable";
import "regenerator-runtime/runtime";
import voteController from "./voteController.js";
import pool from "../config.js";

describe('voteController ', () => {
    test('should give two random characters', () => {
        expect(0).toBe(0);
    });
});